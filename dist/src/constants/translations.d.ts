import { ReservationState } from '../models/Enums';
export default class Translations {
    static reservations: {
        NEW: string;
        CONFIRMED: string;
        CANCELLED: string;
        DONE: string;
        TIME_PAST: string;
    };
}
