/*
import Addresser from '../models/business/addresser/Addresser';

const base_url = 'http://localhost:8080/';


const businesses = {
    mainImage: (addresser: Addresser, index: number) => base_url + 'api/business/main_images/' + JSON.stringify(addresser) + '/' + index,
    armImage: (addresser: Addresser) => base_url + 'api/business/arm/' + JSON.stringify(addresser),
    video: (addresser: Addresser) => base_url + 'api/business/video/' + JSON.stringify(addresser),
};

const products = {
    images: (addresser: Addresser, productId: string, index: number) => base_url + 'api/products/images/' + JSON.stringify(addresser) + '/' + productId + '/' + index,
};

const newsletters = {
    images: (addresser: Addresser, newsletterId: string, index: number) => base_url + 'api/newsletters/images/' + JSON.stringify(addresser) + '/' + newsletterId + '/' + index,
};

export default {
    businesses,
    products,
    newsletters,
};

*/
